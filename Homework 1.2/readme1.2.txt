Read me 
Q.1:take a square-root by heron's method
1.input the number that need to take square-root
2.input significant figures to see the output that stop by stopping criterion

Q.2:convert a floating point number (base-10) into IEEE-754 
1.input the floating point number(base-10) to convert into IEEE-754
2.program will show some of output that have Sign Exponent Mantissa and IEEE-754

Q.3:convert a 32-bit binary string in IEEE-754 format into a decimal floating point number (base-10)
1.input the 32-bit binary string into program 
2.program will show output about Sign of number,Signed exponent,Fraction,and floating point number(base-10)